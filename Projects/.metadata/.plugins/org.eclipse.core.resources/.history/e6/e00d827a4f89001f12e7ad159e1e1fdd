body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f0f0f0; /* Light background for contrast */
}

.register-background {
    background: linear-gradient(to right, #ff7e5f, #feb47b); /* Colorful gradient */
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    position: relative;
    animation: gradientAnimation 15s ease infinite; /* Animation for gradient background */
}

@keyframes gradientAnimation {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}

.content-container {
    display: flex;
    width: 90%;
    max-width: 1200px;
    background: rgba(255, 255, 255, 0.95); /* Slightly transparent background for content */
    border-radius: 15px; /* More rounded corners */
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.4); /* Deeper shadow for better depth */
    overflow: hidden;
}

.text-content {
    flex: 1;
    padding: 40px; /* Increased padding */
    display: flex;
    flex-direction: column;
    justify-content: center;
    text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.7);
}

h1 {
    font-size: 2.5em;
    color: #fff; /* White text for contrast */
    margin-bottom: 20px;
}

.register-container {
    padding: 40px;
    width: 400px;
    text-align: center;
    background: #fff; /* Solid white for the form area */
    box-shadow: 0 2px 15px rgba(0, 0, 0, 0.2); /* Softer shadow for the form area */
    border-radius: 10px;
}

h2 {
    color: #ff6f61;
    margin-bottom: 15px;
}

.info-text {
    font-size: 14px;
    color: #555; /* Dark gray for less emphasis */
    margin-bottom: 20px;
}

.form-group {
    margin-bottom: 20px;
}

label {
    font-size: 16px;
    color: #333;
}

input[type="text"],
input[type="email"],
input[type="password"] {
    width: 100%;
    padding: 12px;
    margin-top: 8px;
    border: 1px solid #ccc;
    border-radius: 5px;
    font-size: 16px;
    transition: border-color 0.3s;
}

input:focus {
    border-color: #ff6f61; /* Highlight border color on focus */
    outline: none; /* Remove default outline */
}

button {
    width: 100%;
    padding: 12px;
    background-color: #ff6f61;
    color: #fff;
    border: none;
    border-radius: 5px;
    font-size: 18px;
    cursor: pointer;
    transition: background-color 0.3s, transform 0.2s; /* Added transform for hover effect */
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2); /* Shadow for button */
}

button:hover {
    background-color: #ff4824; /* Darker shade on hover */
    transform: translateY(-2px); /* Button lift effect */
}

.error {
    color: red;
    font-size: 14px;
    margin-top: 5px;
    background-color: #ffe6e6; /* Light red background for errors */
    padding: 5px; /* Padding for error messages */
    border-radius: 3px; /* Slightly rounded corners */
}

p {
    margin-top: 15px;
    color: #333;
}

p a {
    color: #ff6f61;
    text-decoration: none;
}

p a:hover {
    text-decoration: underline;
}
