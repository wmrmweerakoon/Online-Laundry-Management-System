@charset "UTF-8";
/* Define CSS Variables for easy customization */
:root {
    --primary-color: #ff6f61; /* Coral Pink */
    --secondary-color: #ff9a76; /* Light Coral */
    --accent-color: #ffffff; /* White */
    --hover-color: #ff4824; /* Darker Coral */
    --menu-bg-color: #333; /* Darker gray for menu bar */
    --menu-hover-bg-color: #444; /* Slightly lighter gray for hover */
    --menu-text-color: #ffffff; /* White text for contrast */
    --search-bar-bg: #444; /* Darker background for search bar */
    --bg-color: #181818; /* Dark background for the main section */
    --bg-gradient-start: #292929; /* Dark Gray */
    --bg-gradient-end: #181818; /* Darker Gray */
    --text-primary: #eaeaea; /* Light Gray text */
    --button-bg-color: #ff6f61; /* Coral Pink */
    --button-hover-color: #ff3e0d; /* Darker Coral */
    --card-bg-color: #282828; /* Slightly lighter dark for cards */
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Arial', sans-serif;
    background: linear-gradient(to right, var(--bg-gradient-start), var(--bg-gradient-end));
    color: var(--text-primary);
}

/* Darker Menu Bar */
header {
    background-color: var(--menu-bg-color);
    padding: 15px 0;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
}

nav {
    text-align: center;
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
}

nav ul {
    list-style-type: none;
}

nav ul li {
    display: inline-block;
    margin: 0 20px;
    position: relative;
}

nav ul li a {
    color: var(--menu-text-color);
    text-decoration: none;
    font-size: 18px;
    padding: 12px 20px;
    transition: background-color 0.3s ease, transform 0.2s ease;
    position: relative;
    border-radius: 8px;
}

nav ul li a:hover {
    background-color: var(--menu-hover-bg-color);
    transform: scale(1.05);
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
}

/* Search Bar */
.search-container {
    display: inline-block;
    vertical-align: middle;
    margin-left: 20px;
}

.search-container input {
    padding: 8px;
    font-size: 16px;
    border: none;
    border-radius: 20px;
    width: 230px;
    background-color: var(--search-bar-bg);
    color: var(--accent-color);
    outline: none;
    transition: all 0.3s ease;
}

.search-container input:focus {
    width: 270px;
    background-color: #666;
}

.search-container button {
    padding: 8px 12px;
    background-color: var(--button-bg-color);
    color: var(--accent-color);
    border: none;
    border-radius: 20px;
    cursor: pointer;
    margin-left: 5px;
}

.search-container button:hover {
    background-color: var(--button-hover-color);
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
}

/* Home Section */
.home-section {
    text-align: center;
    padding: 100px 20px;
    background-color: transparent; /* Let the gradient background show */
    position: relative;
}

.slider {
    position: relative;
}

.slider-img {
    width: 110%;
    height: 250px;
    object-fit: cover;
    border-radius: 15px;
    transition: transform 0.3s ease-in-out;
    transform: translateX(-5%);
}

.slider::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.4); /* Dark overlay */
    border-radius: 15px;
}

.welcome-text {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: var(--accent-color);
}

.welcome-text h1 {
    font-size: 42px;
    color: var(--primary-color);
    margin-bottom: 20px;
}

.welcome-text p {
    font-size: 20px;
    margin-bottom: 30px;
}

.welcome-text .highlight {
    color: var(--secondary-color);
}

button {
    padding: 10px 20px;
    font-size: 18px;
    background-color: var(--button-bg-color);
    color: var(--accent-color);
    border: none;
    cursor: pointer;
    border-radius: 20px;
    transition: background-color 0.3s ease, transform 0.3s ease;
}

button:hover {
    background-color: var(--button-hover-color);
    transform: scale(1.05);
}

/* New Information Section Styling */
.info-section {
    text-align: center;
    padding: 50px 20px;
    background-color: var(--bg-color); /* Dark background for info section */
    color: var(--text-primary);
}

.info-cards {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 20px;
}

.info-card {
    background-color: var(--card-bg-color);
    border-radius: 15px;
    padding: 20px;
    width: 250px;
    text-align: center;
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.5);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.info-card i {
    font-size: 40px;
    color: var(--primary-color);
    margin-bottom: 10px;
}

.info-card h3 {
    font-size: 24px;
    color: var(--primary-color);
    margin-bottom: 10px;
}

.info-card p {
    font-size: 16px;
    color: var(--text-primary);
}

.info-card:hover {
    transform: scale(1.05);
    box-shadow: 0 12px 20px rgba(0, 0, 0, 0.7);
}

/* Social Media Links Styling */
.social-media {
    text-align: center;
    padding: 20px 0;
    background-color: var(--menu-bg-color);
}

.social-media a {
    color: var(--accent-color);
    font-size: 24px;
    margin: 0 15px;
    transition: color 0.3s ease;
}

.social-media a:hover {
    color: var(--primary-color);
}
